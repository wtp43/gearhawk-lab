apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: scheduled-backup-infra
  namespace: cnpg-infra
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  suspend: true # S3 egress freeze; restore in 2026-09
  schedule: "0 0 0 * * *" # once per day at midnight, 1st field is seconds
  cluster:
    name: infra-cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  immediate: true
