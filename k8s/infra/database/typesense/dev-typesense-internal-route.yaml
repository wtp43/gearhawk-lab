apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dev-typesense-internal
  namespace: typesense
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - dev-typesense.gearhawk.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: typesense-dev-svc
          port: 8108
