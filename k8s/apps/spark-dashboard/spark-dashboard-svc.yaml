apiVersion: v1
kind: Service
metadata:
  name: spark-dashboard-svc
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: spark-dashboard
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  sessionAffinity: None
