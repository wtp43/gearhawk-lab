apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-dashboard-pvc
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: no-replicas
  volumeMode: Filesystem
