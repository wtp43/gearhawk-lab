apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-openai-pvc
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Gi
  storageClassName: no-replicas
  volumeMode: Filesystem
