apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: solidtime-external
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  parentRefs:
    - name: external
      namespace: gateway
  hostnames:
    - solidtime.dataflarelabs.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: solidtime-svc
          port: 80
