apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: gearhawk
  namespace: argocd
spec:
  sourceRepos:
    - "https://github.com/wtp43/gearhawk-lab"
  destinations:
    - namespace: "argocd"
      server: "*"
    # Kept so ArgoCD can prune the old single-namespace deployment.
    - namespace: "gearhawk-website"
      server: "*"
    - namespace: "api-gearhawk"
      server: "*"
    - namespace: "web-gearhawk"
      server: "*"
    - namespace: "api-gearhawk-dev"
      server: "*"
    - namespace: "web-gearhawk-dev"
      server: "*"
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
