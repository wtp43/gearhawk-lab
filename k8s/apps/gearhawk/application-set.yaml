apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gearhawk
  namespace: argocd
  labels:
    gearhawk.lab: gearhawk
spec:
  generators:
    - git:
        repoURL: https://github.com/wtp43/gearhawk-lab
        revision: HEAD
        directories:
          - path: k8s/apps/gearhawk/*
  template:
    metadata:
      name: "{{ path.basename }}"
      labels:
        gearhawk.lab: gearhawk
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: applications
      source:
        repoURL: https://github.com/wtp43/gearhawk-lab
        targetRevision: HEAD
        path: "{{ path }}"
      destination:
        name: in-cluster
        namespace: "{{ path.basename }}"
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
